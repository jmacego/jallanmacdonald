{
  "name": "jallanmacdonald",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir apps/site dev",
    "build:site": "ASTRO_TELEMETRY_DISABLED=1 XDG_CONFIG_HOME=/tmp pnpm --dir apps/site build",
    "preview": "pnpm --dir apps/site preview",
    "validate": "./scripts/validate.sh",
    "deploy:aws": "bash -c 'ASTRO_TELEMETRY_DISABLED=1 XDG_CONFIG_HOME=/tmp pnpm --dir apps/site build && set -a; if [ -f .env ]; then source .env; else echo \"Missing .env\"; exit 1; fi; set +a; pnpm --dir infra exec cdk deploy \"$STACK_NAME\" --require-approval never'"
  }
}
